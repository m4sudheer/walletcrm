# walletcrm (NestJS sample)

This is a minimal NestJS TypeScript project with a single GET endpoint that returns "Hello World!".

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run in development mode (auto-restart on changes):

```bash
npm run start:dev
```

3. Open http://localhost:3000/ — you should see "Hello World!" as the response.

Build and run the compiled app:

```bash
npm run build
npm start
```
# walletcrm